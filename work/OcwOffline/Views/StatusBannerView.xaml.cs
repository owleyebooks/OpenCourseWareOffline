namespace OcwOffline.Views;

// Bound to the shared AppStatusViewModel singleton by each page that
// includes it. Code-behind only; the XAML carries the bindings.
public partial class StatusBannerView : ContentView
{
    public StatusBannerView()
    {
        InitializeComponent();
    }
}
